// Q4. Generate Numbers Between Any Two Given Numbers.
// Here’s a program that generates numbers between two given numbers:

const num1 = 10;
const num2 = 25;

for (let i = num1 + 1; i < num2; i++) {
  console.log(i);
}
